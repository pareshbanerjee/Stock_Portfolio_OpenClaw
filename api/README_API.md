API serverless functions placeholder

This folder is intended for Vercel Serverless Functions (Python). Add one file per endpoint.

Example:
- `api/portfolio_get.py` -> responds to GET /api/portfolio
- `api/portfolio_stock_post.py` -> handles POST /api/portfolio/stock

See README_Vercel.md for details and an example Starlette app snippet.
